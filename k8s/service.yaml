apiVersion: v1
kind: Service
metadata:
  name: demystops-service
  namespace: demystops
spec:
  selector:
    app: demystops
  ports:
  - port: 8081
    targetPort: 8081
    nodePort: 30081
  type: NodePort